#include <stdio.h>
/**
  * main - Entry Point
  * Description: 'Prints number of base ten'
  * Return: Always 0 (Success)
  */
int main(void)
{
	int n;

	for (n = 0; n <= 9; n++)
	{
		printf("%d", n);
	}
	print('\n');
	return (0);
}
