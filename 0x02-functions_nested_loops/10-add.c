#include "main.h"
/**
 * add - 'Adding two intergers and returning results'
 * @f: an integer to be added.
 * @g: an integer to be added.
 *
 * Return: sum of f and g.
 */
int add(int f, int g)
{
	int c;

	c = f + g;

	return (c);
}
